#include <bits/stdc++.h>
using namespace std;

void unordered_sets(){

    unordered set<int> st;

    //unique
    //not sorted

    // lower_bound() and upper_bound() functions
    // does not works, rest all functions are same
    // no particular order so has better complexity

}