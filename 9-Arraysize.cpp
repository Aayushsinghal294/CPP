#include<bits/stdc++.h>
using namespace std;

     const int n = 1e7;     //  For using global... we should use const keyword so that value remain fixed
    int a[n];               // For this we can use global array as its ramge of order 10^7


int main(){

   // int n = 1e7;            // this is not showing output as local array size is around 10^5 but we took 1e7
   // int a[n];
   
    a[n-1] = 7;
    cout << a[n-1];


}